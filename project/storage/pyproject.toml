[project]
name = "rag-storage"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=0.5",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/storage"]
